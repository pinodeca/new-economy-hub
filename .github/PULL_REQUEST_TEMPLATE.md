## What does this change?

<!-- One or two sentences. -->

## If this adds or edits a file under `content/`

See [content/README.md](../content/README.md) for the full conventions. At minimum:

- [ ] Checked existing `content/` files for a duplicate/near-duplicate before adding a new one
- [ ] `sourceType` is set correctly, and `verified` is `false` (merging this PR is the verification step — see [CLAUDE.md](../CLAUDE.md))
- [ ] Sources for any factual claims (member counts, mission statements, dates) are listed below, so this is checkable without re-doing the research

**Sources:**
<!-- URLs you researched, not just "researched online" -->

## If this is a code change

- [ ] `npm run lint` and `npm run build` pass locally
- [ ] Checked the change in a browser if it touches UI (this is a static export — `output: "export"` — so no server-only behavior)
